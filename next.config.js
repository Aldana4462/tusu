/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    // si vas a usar Tailwind JIT,
    // puedes anadir configuraciones adicionales aqui
  }
}
module.exports = nextConfig
